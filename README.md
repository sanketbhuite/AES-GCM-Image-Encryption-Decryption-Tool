# AES-GCM-Image-Encryption-Decryption-Tool
A modern Windows desktop application built using Java Swing that allows users to securely encrypt and decrypt image files using AES-GCM System Security Algorithm
